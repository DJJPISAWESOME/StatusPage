self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil((async()=>{const clients=await self.clients.matchAll({type:'window',includeUncontrolled:true});const current=clients.find(c=>new URL(c.url).origin===self.location.origin);if(current){await current.focus();await current.navigate('/#services');}else await self.clients.openWindow('/#services');})());});
